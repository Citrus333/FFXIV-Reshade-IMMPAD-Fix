Drop into your game\reshade-shaders\Shaders folder.

In the settings there is a "use true engine normals" setting that allows you to set the upscale modifier for DLSS
-For example if using Quality DLSS, use 0.667
